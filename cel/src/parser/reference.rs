#[derive(Clone, Debug, PartialEq)]
pub enum Val {
    Unspecified,
    Dyn,
    Any,
    Boolean(bool),
    Bytes(Vec<u8>),
    Double(f64),
    Duration,
    Error,
    Int(i64),
    List,
    Map,
    Null,
    Opaque,
    String(String),
    Struct,
    Timestamp,
    Type,
    TypeParam,
    UInt(u64),
    Unknown,
}
