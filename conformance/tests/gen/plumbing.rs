// @generated by `cargo run -p conformance --bin generate`.
// DO NOT EDIT MANUALLY.

// Source file: plumbing
// Description: Check that the ConformanceService server can accept all arguments and return all responses.

// Section: min
// Minimal programs.
mod min {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: min_program
    #[test]
    fn min_program() {
        run_test(&dedent!(
            r#"
                description: "Smallest functionality: expr in, result out."
                expr: "17"
                value: { int64_value: 17 }
            "#
        ));
    }
}

// Section: eval_results
// All evaluation result kinds.
mod eval_results {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: error_result
    #[test]
    fn error_result() {
        run_test(&dedent!(
            r#"
                description: "Check that error results go through."
                expr: "1 / 0"
                eval_error: {
                  errors: { message: "foo" }
                }
            "#
        ));
    }

    // Test: eval_map_results
    #[test]
    fn eval_map_results() {
        run_test(&dedent!(
            r#"
                description: "Check that map literals results are order independent."
                expr: '{"k1":"v1","k":"v"}'
                value: {
                  map_value {
                    entries {
                      key: { string_value: "k" }
                      value: { string_value: "v" }
                    }
                    entries {
                      key: { string_value: "k1" }
                      value: { string_value: "v1" }
                    }
                  }
                }
            "#
        ));
    }
}

// Section: check_inputs
// All inputs to Check phase.
mod check_inputs {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: skip_check
    #[test]
    fn skip_check() {
        run_test(&dedent!(
            r#"
                description: "Make sure we can skip type checking."
                expr: "[17, 'pancakes']"
                disable_check: true
                value: {
                  list_value {
                    values: { int64_value: 17 }
                    values: { string_value: "pancakes" }
                  }
                }
            "#
        ));
    }
}

// Section: eval_inputs
// All inputs to Eval phase.
mod eval_inputs {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: one_ignored_value_arg
    #[test]
    fn one_ignored_value_arg() {
        run_test(&dedent!(
            r#"
                description: "Check that value bindings can be given, even if ignored."
                expr: "'foo'"
                bindings: {
                  key: "x"
                  value: { value: { int64_value: 17 } }
                }
                value: { string_value: "foo" }
            "#
        ));
    }
}
