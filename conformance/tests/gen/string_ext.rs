// @generated by `cargo run -p conformance --bin generate`.
// DO NOT EDIT MANUALLY.

// Source file: string_ext
// Description: Tests for the strings extension library.

// Section: char_at
mod char_at {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: middle_index
    #[should_panic]
    #[test]
    fn middle_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.charAt(3)"
                value: {
                  string_value: "o"
                }
            "#
        ));
    }

    // Test: end_index
    #[should_panic]
    #[test]
    fn end_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.charAt(7)"
                value: {
                  string_value: ""
                }
            "#
        ));
    }

    // Test: multiple
    #[should_panic]
    #[test]
    fn multiple() {
        run_test(&dedent!(
            r#"
                expr: "'©αT'.charAt(0) == '©' && '©αT'.charAt(1) == 'α' && '©αT'.charAt(2) == 'T'"
            "#
        ));
    }
}

// Section: index_of
mod index_of {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty_index
    #[should_panic]
    #[test]
    fn empty_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('')"
                value: {
                  int64_value: 0
                }
            "#
        ));
    }

    // Test: string_index
    #[should_panic]
    #[test]
    fn string_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('ac')"
                value: {
                  int64_value: 1
                }
            "#
        ));
    }

    // Test: nomatch
    #[should_panic]
    #[test]
    fn nomatch() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('none') == -1"
            "#
        ));
    }

    // Test: empty_index
    #[should_panic]
    #[test]
    fn empty_index_1() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('', 3) == 3"
            "#
        ));
    }

    // Test: char_index
    #[should_panic]
    #[test]
    fn char_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('a', 3) == 5"
            "#
        ));
    }

    // Test: string_index
    #[should_panic]
    #[test]
    fn string_index_1() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('at', 3) == 5"
            "#
        ));
    }

    // Test: unicode_char
    #[should_panic]
    #[test]
    fn unicode_char() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.indexOf('©') == 2"
            "#
        ));
    }

    // Test: unicode_char_index
    #[should_panic]
    #[test]
    fn unicode_char_index() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.indexOf('©', 3) == 4"
            "#
        ));
    }

    // Test: unicode_string_index
    #[should_panic]
    #[test]
    fn unicode_string_index() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.indexOf('©αT', 3) == 4"
            "#
        ));
    }

    // Test: unicode_string_nomatch_index
    #[should_panic]
    #[test]
    fn unicode_string_nomatch_index() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.indexOf('©α', 5) == -1"
            "#
        ));
    }

    // Test: char_index
    #[should_panic]
    #[test]
    fn char_index_1() {
        run_test(&dedent!(
            r#"
                expr: "'ijk'.indexOf('k') == 2"
            "#
        ));
    }

    // Test: string_with_space_fullmatch
    #[should_panic]
    #[test]
    fn string_with_space_fullmatch() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.indexOf('hello wello') == 0"
            "#
        ));
    }

    // Test: string_with_space_index
    #[should_panic]
    #[test]
    fn string_with_space_index() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.indexOf('ello', 6) == 7"
            "#
        ));
    }

    // Test: string_nomatch_index
    #[should_panic]
    #[test]
    fn string_nomatch_index() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.indexOf('elbo room!!') == -1"
            "#
        ));
    }
}

// Section: last_index_of
mod last_index_of {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty_string
    #[should_panic]
    #[test]
    fn empty_string() {
        run_test(&dedent!(
            r#"
                expr: "''.lastIndexOf('@@') == -1"
            "#
        ));
    }

    // Test: empty_argument
    #[should_panic]
    #[test]
    fn empty_argument() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('') == 7"
            "#
        ));
    }

    // Test: string
    #[should_panic]
    #[test]
    fn string() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('at') == 5"
            "#
        ));
    }

    // Test: string_nomatch
    #[should_panic]
    #[test]
    fn string_nomatch() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('none') == -1"
            "#
        ));
    }

    // Test: empty_index
    #[should_panic]
    #[test]
    fn empty_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('', 3) == 3"
            "#
        ));
    }

    // Test: char_index
    #[should_panic]
    #[test]
    fn char_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('a', 3) == 1"
            "#
        ));
    }

    // Test: unicode_char
    #[should_panic]
    #[test]
    fn unicode_char() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.lastIndexOf('©') == 4"
            "#
        ));
    }

    // Test: unicode_char_index
    #[should_panic]
    #[test]
    fn unicode_char_index() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.lastIndexOf('©', 3) == 2"
            "#
        ));
    }

    // Test: unicode_string_index
    #[should_panic]
    #[test]
    fn unicode_string_index() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.lastIndexOf('©α', 4) == 4"
            "#
        ));
    }

    // Test: string_with_space_string_index
    #[should_panic]
    #[test]
    fn string_with_space_string_index() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.lastIndexOf('ello', 6) == 1"
            "#
        ));
    }

    // Test: string_with_space_string_nomatch
    #[should_panic]
    #[test]
    fn string_with_space_string_nomatch() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.lastIndexOf('low') == -1"
            "#
        ));
    }

    // Test: string_with_space_string_with_space_nomatch
    #[should_panic]
    #[test]
    fn string_with_space_string_with_space_nomatch() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.lastIndexOf('elbo room!!') == -1"
            "#
        ));
    }

    // Test: string_with_space_fullmatch
    #[should_panic]
    #[test]
    fn string_with_space_fullmatch() {
        run_test(&dedent!(
            r#"
                expr: "'hello wello'.lastIndexOf('hello wello') == 0"
            "#
        ));
    }

    // Test: repeated_string
    #[should_panic]
    #[test]
    fn repeated_string() {
        run_test(&dedent!(
            r#"
                expr: "'bananananana'.lastIndexOf('nana', 7) == 6"
            "#
        ));
    }
}

// Section: ascii_casing
mod ascii_casing {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: lowerascii
    #[should_panic]
    #[test]
    fn lowerascii() {
        run_test(&dedent!(
            r#"
                expr: "'TacoCat'.lowerAscii() == 'tacocat'"
            "#
        ));
    }

    // Test: lowerascii_unicode
    #[should_panic]
    #[test]
    fn lowerascii_unicode() {
        run_test(&dedent!(
            r#"
                expr: "'TacoCÆt'.lowerAscii() == 'tacocÆt'"
            "#
        ));
    }

    // Test: lowerascii_unicode_with_space
    #[should_panic]
    #[test]
    fn lowerascii_unicode_with_space() {
        run_test(&dedent!(
            r#"
                expr: "'TacoCÆt Xii'.lowerAscii() == 'tacocÆt xii'"
            "#
        ));
    }

    // Test: upperascii
    #[should_panic]
    #[test]
    fn upperascii() {
        run_test(&dedent!(
            r#"
                expr: "'tacoCat'.upperAscii() == 'TACOCAT'"
            "#
        ));
    }

    // Test: upperascii_unicode
    #[should_panic]
    #[test]
    fn upperascii_unicode() {
        run_test(&dedent!(
            r#"
                expr: "'tacoCαt'.upperAscii() == 'TACOCαT'"
            "#
        ));
    }

    // Test: upperascii_unicode_with_space
    #[should_panic]
    #[test]
    fn upperascii_unicode_with_space() {
        run_test(&dedent!(
            r#"
                expr: "'TacoCÆt Xii'.upperAscii() == 'TACOCÆT XII'"
            "#
        ));
    }
}

// Section: replace
mod replace {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: no_placeholder
    #[should_panic]
    #[test]
    fn no_placeholder() {
        run_test(&dedent!(
            r#"
                expr: "'12 days 12 hours'.replace('{0}', '2') == '12 days 12 hours'"
            "#
        ));
    }

    // Test: basic
    #[should_panic]
    #[test]
    fn basic() {
        run_test(&dedent!(
            r#"
                expr: "'{0} days {0} hours'.replace('{0}', '2') == '2 days 2 hours'"
            "#
        ));
    }

    // Test: chained
    #[should_panic]
    #[test]
    fn chained() {
        run_test(&dedent!(
            r#"
                expr: "'{0} days {0} hours'.replace('{0}', '2', 1).replace('{0}', '23') == '2 days 23 hours'"
            "#
        ));
    }

    // Test: unicode
    #[should_panic]
    #[test]
    fn unicode() {
        run_test(&dedent!(
            r#"
                expr: "'1 ©αT taco'.replace('αT', 'o©α') == '1 ©o©α taco'"
            "#
        ));
    }
}

// Section: split
mod split {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty
    #[should_panic]
    #[test]
    fn empty() {
        run_test(&dedent!(
            r#"
                expr: "'hello world'.split(' ') == ['hello', 'world']"
            "#
        ));
    }

    // Test: zero_limit
    #[should_panic]
    #[test]
    fn zero_limit() {
        run_test(&dedent!(
            r#"
                expr: "'hello world events!'.split(' ', 0) == []"
            "#
        ));
    }

    // Test: one_limit
    #[should_panic]
    #[test]
    fn one_limit() {
        run_test(&dedent!(
            r#"
                expr: "'hello world events!'.split(' ', 1) == ['hello world events!']"
            "#
        ));
    }

    // Test: unicode_negative_limit
    #[should_panic]
    #[test]
    fn unicode_negative_limit() {
        run_test(&dedent!(
            r#"
                expr: "'o©o©o©o'.split('©', -1) == ['o', 'o', 'o', 'o']"
            "#
        ));
    }
}

// Section: substring
mod substring {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: start
    #[should_panic]
    #[test]
    fn start() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(4) == 'cat'"
            "#
        ));
    }

    // Test: start_with_max_length
    #[should_panic]
    #[test]
    fn start_with_max_length() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(7) == ''"
            "#
        ));
    }

    // Test: start_and_end
    #[should_panic]
    #[test]
    fn start_and_end() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(0, 4) == 'taco'"
            "#
        ));
    }

    // Test: start_and_end_equal_value
    #[should_panic]
    #[test]
    fn start_and_end_equal_value() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(4, 4) == ''"
            "#
        ));
    }

    // Test: unicode_start_and_end
    #[should_panic]
    #[test]
    fn unicode_start_and_end() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.substring(2, 6) == '©o©α'"
            "#
        ));
    }

    // Test: unicode_start_and_end_equal_value
    #[should_panic]
    #[test]
    fn unicode_start_and_end_equal_value() {
        run_test(&dedent!(
            r#"
                expr: "'ta©o©αT'.substring(7, 7) == ''"
            "#
        ));
    }
}

// Section: trim
mod trim {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: blank_spaces_escaped_chars
    #[should_panic]
    #[test]
    fn blank_spaces_escaped_chars() {
        run_test(&dedent!(
            r#"
                expr: "' \\f\\n\\r\\t\\vtext  '.trim() == 'text'"
            "#
        ));
    }

    // Test: unicode_space_chars_1
    #[should_panic]
    #[test]
    fn unicode_space_chars_1() {
        run_test(&dedent!(
            r#"
                expr: "'\\u0085\\u00a0\\u1680text'.trim() == 'text'"
            "#
        ));
    }

    // Test: unicode_space_chars_2
    #[should_panic]
    #[test]
    fn unicode_space_chars_2() {
        run_test(&dedent!(
            r#"
                expr: "'text\\u2000\\u2001\\u2002\\u2003\\u2004\\u2004\\u2006\\u2007\\u2008\\u2009'.trim() == 'text'"
            "#
        ));
    }

    // Test: unicode_space_chars_3
    #[should_panic]
    #[test]
    fn unicode_space_chars_3() {
        run_test(&dedent!(
            r#"
                expr: "'\\u200atext\\u2028\\u2029\\u202F\\u205F\\u3000'.trim() == 'text'"
            "#
        ));
    }

    // Test: unicode_no_trim
    #[should_panic]
    #[test]
    fn unicode_no_trim() {
        run_test(&dedent!(
            r#"
                expr: "'\\u180etext\\u200b\\u200c\\u200d\\u2060\\ufeff'.trim() == '\\u180etext\\u200b\\u200c\\u200d\\u2060\\ufeff'"
            "#
        ));
    }
}

// Section: join
mod join {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty_separator
    #[should_panic]
    #[test]
    fn empty_separator() {
        run_test(&dedent!(
            r#"
                expr: "['x', 'y'].join() == 'xy'"
            "#
        ));
    }

    // Test: dash_separator
    #[should_panic]
    #[test]
    fn dash_separator() {
        run_test(&dedent!(
            r#"
                expr: "['x', 'y'].join('-') == 'x-y'"
            "#
        ));
    }

    // Test: empty_string_empty_separator
    #[should_panic]
    #[test]
    fn empty_string_empty_separator() {
        run_test(&dedent!(
            r#"
                expr: "[].join() == ''"
            "#
        ));
    }

    // Test: empty_string_dash_separator
    #[should_panic]
    #[test]
    fn empty_string_dash_separator() {
        run_test(&dedent!(
            r#"
                expr: "[].join('-') == ''"
            "#
        ));
    }
}

// Section: quote
mod quote {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: multiline
    #[should_panic]
    #[test]
    fn multiline() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"first\\nsecond\") == \"\\\"first\\\\nsecond\\\"\""
            "#
        ));
    }

    // Test: escaped
    #[should_panic]
    #[test]
    fn escaped() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"bell\\a\") == \"\\\"bell\\\\a\\\"\""
            "#
        ));
    }

    // Test: backspace
    #[should_panic]
    #[test]
    fn backspace() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"\\bbackspace\") == \"\\\"\\\\bbackspace\\\"\""
            "#
        ));
    }

    // Test: form_feed
    #[should_panic]
    #[test]
    fn form_feed() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"\\fform feed\") == \"\\\"\\\\fform feed\\\"\""
            "#
        ));
    }

    // Test: carriage_return
    #[should_panic]
    #[test]
    fn carriage_return() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"carriage \\r return\") == \"\\\"carriage \\\\r return\\\"\""
            "#
        ));
    }

    // Test: horizontal_tab
    #[should_panic]
    #[test]
    fn horizontal_tab() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"horizontal tab\\t\") == \"\\\"horizontal tab\\\\t\\\"\""
            "#
        ));
    }

    // Test: vertical_tab
    #[should_panic]
    #[test]
    fn vertical_tab() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"vertical \\v tab\") == \"\\\"vertical \\\\v tab\\\"\""
            "#
        ));
    }

    // Test: double_slash
    #[should_panic]
    #[test]
    fn double_slash() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"double \\\\\\\\ slash\") == \"\\\"double \\\\\\\\\\\\\\\\ slash\\\"\""
            "#
        ));
    }

    // Test: two_escape_sequences
    #[should_panic]
    #[test]
    fn two_escape_sequences() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"two escape sequences \\\\a\\\\n\") == \"\\\"two escape sequences \\\\\\\\a\\\\\\\\n\\\"\""
            "#
        ));
    }

    // Test: verbatim
    #[should_panic]
    #[test]
    fn verbatim() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"verbatim\") == \"\\\"verbatim\\\"\""
            "#
        ));
    }

    // Test: ends_with
    #[should_panic]
    #[test]
    fn ends_with() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"ends with \\\\\") == \"\\\"ends with \\\\\\\\\\\"\""
            "#
        ));
    }

    // Test: starts_with
    #[should_panic]
    #[test]
    fn starts_with() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"\\\\ starts with\") == \"\\\"\\\\\\\\ starts with\\\"\""
            "#
        ));
    }

    // Test: printable_unicode
    #[should_panic]
    #[test]
    fn printable_unicode() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"printable unicode😀\") == \"\\\"printable unicode😀\\\"\""
            "#
        ));
    }

    // Test: mid_string_quote
    #[should_panic]
    #[test]
    fn mid_string_quote() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"mid string \\\" quote\") == \"\\\"mid string \\\\\\\" quote\\\"\""
            "#
        ));
    }

    // Test: single_quote_with_double_quote
    #[should_panic]
    #[test]
    fn single_quote_with_double_quote() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote('single-quote with \"double quote\"') == \"\\\"single-quote with \\\\\\\"double quote\\\\\\\"\\\"\""
            "#
        ));
    }

    // Test: size_unicode_char
    #[should_panic]
    #[test]
    fn size_unicode_char() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"size('ÿ')\") == \"\\\"size('ÿ')\\\"\""
            "#
        ));
    }

    // Test: size_unicode_string
    #[should_panic]
    #[test]
    fn size_unicode_string() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"size('πέντε')\") == \"\\\"size('πέντε')\\\"\""
            "#
        ));
    }

    // Test: unicode
    #[should_panic]
    #[test]
    fn unicode() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"завтра\") == \"\\\"завтра\\\"\""
            "#
        ));
    }

    // Test: unicode_code_points
    #[should_panic]
    #[test]
    fn unicode_code_points() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"\\U0001F431\\U0001F600\\U0001F61B\")"
                value: {
                  string_value: "\"🐱😀😛\""
                }
            "#
        ));
    }

    // Test: unicode_2
    #[should_panic]
    #[test]
    fn unicode_2() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"ta©o©αT\") == \"\\\"ta©o©αT\\\"\""
            "#
        ));
    }

    // Test: empty_quote
    #[should_panic]
    #[test]
    fn empty_quote() {
        run_test(&dedent!(
            r#"
                expr: "strings.quote(\"\")"
                value: {
                  string_value: "\"\""
                }
            "#
        ));
    }
}

// Section: format
mod format {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: no-op
    #[should_panic]
    #[test]
    fn no_op() {
        run_test(&dedent!(
            r#"
                expr: '"no substitution".format([])'
                value: {
                  string_value: 'no substitution',
                }
            "#
        ));
    }

    // Test: mid-string substitution
    #[should_panic]
    #[test]
    fn mid_string_substitution() {
        run_test(&dedent!(
            r#"
                expr: '"str is %s and some more".format(["filler"])'
                value: {
                  string_value: 'str is filler and some more',
                }
            "#
        ));
    }

    // Test: percent escaping
    #[should_panic]
    #[test]
    fn percent_escaping() {
        run_test(&dedent!(
            r#"
                expr: '"%% and also %%".format([])'
                value: {
                  string_value: '% and also %',
                }
            "#
        ));
    }

    // Test: substitution inside escaped percent signs
    #[should_panic]
    #[test]
    fn substitution_inside_escaped_percent_signs() {
        run_test(&dedent!(
            r#"
                expr: '"%%%s%%".format(["text"])'
                value: {
                  string_value: '%text%',
                }
            "#
        ));
    }

    // Test: substitution with one escaped percent sign on the right
    #[should_panic]
    #[test]
    fn substitution_with_one_escaped_percent_sign_on_the_right() {
        run_test(&dedent!(
            r#"
                expr: '"%s%%".format(["percent on the right"])'
                value: {
                  string_value: 'percent on the right%',
                }
            "#
        ));
    }

    // Test: substitution with one escaped percent sign on the left
    #[should_panic]
    #[test]
    fn substitution_with_one_escaped_percent_sign_on_the_left() {
        run_test(&dedent!(
            r#"
                expr: '"%%%s".format(["percent on the left"])'
                value: {
                  string_value: '%percent on the left',
                }
            "#
        ));
    }

    // Test: multiple substitutions
    #[should_panic]
    #[test]
    fn multiple_substitutions() {
        run_test(&dedent!(
            r#"
                expr: '"%d %d %d, %s %s %s, %d %d %d, %s %s %s".format([1, 2, 3, "A", "B", "C", 4, 5, 6, "D", "E", "F"])'
                value: {
                  string_value: '1 2 3, A B C, 4 5 6, D E F',
                }
            "#
        ));
    }

    // Test: percent sign escape sequence support
    #[should_panic]
    #[test]
    fn percent_sign_escape_sequence_support() {
        run_test(&dedent!(
            r#"
                expr: '"%%escaped %s%%".format(["percent"])'
                value: {
                  string_value: '%escaped percent%',
                }
            "#
        ));
    }

    // Test: fixed point formatting clause
    #[should_panic]
    #[test]
    fn fixed_point_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%.3f".format([1.2345])'
                value: {
                  string_value: '1.234',
                }
            "#
        ));
    }

    // Test: binary formatting clause
    #[should_panic]
    #[test]
    fn binary_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"this is 5 in binary: %b".format([5])'
                value: {
                  string_value: 'this is 5 in binary: 101',
                }
            "#
        ));
    }

    // Test: uint support for binary formatting
    #[should_panic]
    #[test]
    fn uint_support_for_binary_formatting() {
        run_test(&dedent!(
            r#"
                expr: '"unsigned 64 in binary: %b".format([uint(64)])'
                value: {
                  string_value: 'unsigned 64 in binary: 1000000',
                }
            "#
        ));
    }

    // Test: bool support for binary formatting
    #[should_panic]
    #[test]
    fn bool_support_for_binary_formatting() {
        run_test(&dedent!(
            r#"
                expr: '"bit set from bool: %b".format([true])'
                value: {
                  string_value: 'bit set from bool: 1',
                }
            "#
        ));
    }

    // Test: octal formatting clause
    #[should_panic]
    #[test]
    fn octal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%o".format([11])'
                value: {
                  string_value: '13',
                }
            "#
        ));
    }

    // Test: uint support for octal formatting clause
    #[should_panic]
    #[test]
    fn uint_support_for_octal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"this is an unsigned octal: %o".format([uint(65535)])'
                value: {
                  string_value: 'this is an unsigned octal: 177777',
                }
            "#
        ));
    }

    // Test: lowercase hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn lowercase_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%x is 20 in hexadecimal".format([30])'
                value: {
                  string_value: '1e is 20 in hexadecimal',
                }
            "#
        ));
    }

    // Test: uppercase hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn uppercase_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%X is 20 in hexadecimal".format([30])'
                value: {
                  string_value: '1E is 20 in hexadecimal',
                }
            "#
        ));
    }

    // Test: unsigned support for hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn unsigned_support_for_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%X is 6000 in hexadecimal".format([uint(6000)])'
                value: {
                  string_value: '1770 is 6000 in hexadecimal',
                }
            "#
        ));
    }

    // Test: string support with hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn string_support_with_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%x".format(["Hello world!"])'
                value: {
                  string_value: '48656c6c6f20776f726c6421',
                }
            "#
        ));
    }

    // Test: string support with uppercase hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn string_support_with_uppercase_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%X".format(["Hello world!"])'
                value: {
                  string_value: '48656C6C6F20776F726C6421',
                }
            "#
        ));
    }

    // Test: byte support with hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn byte_support_with_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%x".format([b"byte string"])'
                value: {
                  string_value: '6279746520737472696e67',
                }
            "#
        ));
    }

    // Test: byte support with uppercase hexadecimal formatting clause
    #[should_panic]
    #[test]
    fn byte_support_with_uppercase_hexadecimal_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%X".format([b"byte string"])'
                value: {
                  string_value: '6279746520737472696E67',
                }
            "#
        ));
    }

    // Test: scientific notation formatting clause
    #[should_panic]
    #[test]
    fn scientific_notation_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%.6e".format([1052.032911275])'
                value: {
                  string_value: '1.052033e+03',
                }
            "#
        ));
    }

    // Test: default precision for fixed-point clause
    #[should_panic]
    #[test]
    fn default_precision_for_fixed_point_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%f".format([2.71828])'
                value: {
                  string_value: '2.718280',
                }
            "#
        ));
    }

    // Test: default precision for scientific notation
    #[should_panic]
    #[test]
    fn default_precision_for_scientific_notation() {
        run_test(&dedent!(
            r#"
                expr: '"%e".format([2.71828])'
                value: {
                  string_value: '2.718280e+00',
                }
            "#
        ));
    }

    // Test: NaN support for scientific notation
    #[should_panic]
    #[test]
    fn nan_support_for_scientific_notation() {
        run_test(&dedent!(
            r#"
                expr: '"%e".format([double("NaN")])'
                value: {
                  string_value: 'NaN',
                }
            "#
        ));
    }

    // Test: positive infinity support for scientific notation
    #[should_panic]
    #[test]
    fn positive_infinity_support_for_scientific_notation() {
        run_test(&dedent!(
            r#"
                expr: '"%e".format([double("Infinity")])'
                value: {
                  string_value: 'Infinity',
                }
            "#
        ));
    }

    // Test: negative infinity support for scientific notation
    #[should_panic]
    #[test]
    fn negative_infinity_support_for_scientific_notation() {
        run_test(&dedent!(
            r#"
                expr: '"%e".format([double("-Infinity")])'
                value: {
                  string_value: '-Infinity',
                }
            "#
        ));
    }

    // Test: NaN support for decimal
    #[should_panic]
    #[test]
    fn nan_support_for_decimal() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([double("NaN")])'
                value: {
                  string_value: 'NaN',
                }
            "#
        ));
    }

    // Test: positive infinity support for decimal
    #[should_panic]
    #[test]
    fn positive_infinity_support_for_decimal() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([double("Infinity")])'
                value: {
                  string_value: 'Infinity',
                }
            "#
        ));
    }

    // Test: negative infinity support for decimal
    #[should_panic]
    #[test]
    fn negative_infinity_support_for_decimal() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([double("-Infinity")])'
                value: {
                  string_value: '-Infinity',
                }
            "#
        ));
    }

    // Test: NaN support for fixed-point
    #[should_panic]
    #[test]
    fn nan_support_for_fixed_point() {
        run_test(&dedent!(
            r#"
                expr: '"%f".format([double("NaN")])'
                value: {
                  string_value: 'NaN',
                }
            "#
        ));
    }

    // Test: positive infinity support for fixed-point
    #[should_panic]
    #[test]
    fn positive_infinity_support_for_fixed_point() {
        run_test(&dedent!(
            r#"
                expr: '"%f".format([double("Infinity")])'
                value: {
                  string_value: 'Infinity',
                }
            "#
        ));
    }

    // Test: negative infinity support for fixed-point
    #[should_panic]
    #[test]
    fn negative_infinity_support_for_fixed_point() {
        run_test(&dedent!(
            r#"
                expr: '"%f".format([double("-Infinity")])'
                value: {
                  string_value: '-Infinity',
                }
            "#
        ));
    }

    // Test: uint support for decimal clause
    #[should_panic]
    #[test]
    fn uint_support_for_decimal_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([uint(64)])'
                value: {
                  string_value: '64',
                }
            "#
        ));
    }

    // Test: null support for string
    #[should_panic]
    #[test]
    fn null_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([null])'
                value: {
                  string_value: 'null',
                }
            "#
        ));
    }

    // Test: int support for string
    #[should_panic]
    #[test]
    fn int_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([999999999999])'
                value: {
                  string_value: '999999999999',
                }
            "#
        ));
    }

    // Test: bytes support for string
    #[should_panic]
    #[test]
    fn bytes_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([b"xyz"])'
                value: {
                  string_value: 'xyz',
                }
            "#
        ));
    }

    // Test: type() support for string
    #[should_panic]
    #[test]
    fn type_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([type("test string")])'
                value: {
                  string_value: 'string',
                }
            "#
        ));
    }

    // Test: timestamp support for string
    #[should_panic]
    #[test]
    fn timestamp_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([timestamp("2023-02-03T23:31:20+00:00")])'
                value: {
                  string_value: '2023-02-03T23:31:20Z',
                }
            "#
        ));
    }

    // Test: duration support for string
    #[should_panic]
    #[test]
    fn duration_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([duration("1h45m47s")])'
                value: {
                  string_value: '6347s',
                }
            "#
        ));
    }

    // Test: list support for string
    #[should_panic]
    #[test]
    fn list_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([["abc", 3.14, null, [9, 8, 7, 6], timestamp("2023-02-03T23:31:20Z")]])'
                value: {
                  string_value: '[abc, 3.14, null, [9, 8, 7, 6], 2023-02-03T23:31:20Z]',
                }
            "#
        ));
    }

    // Test: map support for string
    #[should_panic]
    #[test]
    fn map_support_for_string() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([{"key1": b"xyz", "key5": null, "key2": duration("2h"), "key4": true, "key3": 2.71828}])'
                value: {
                  string_value: '{key1: xyz, key2: 7200s, key3: 2.71828, key4: true, key5: null}',
                }
            "#
        ));
    }

    // Test: map support (all key types)
    #[should_panic]
    #[test]
    fn map_support_all_key_types() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([{1: "value1", uint(2): "value2", true: double("NaN")}])'
                value: {
                  string_value: '{1: value1, 2: value2, true: NaN}',
                }
            "#
        ));
    }

    // Test: boolean support for %s
    #[should_panic]
    #[test]
    fn boolean_support_for_s() {
        run_test(&dedent!(
            r#"
                expr: '"%s, %s".format([true, false])'
                value: {
                  string_value: 'true, false',
                }
            "#
        ));
    }

    // Test: dyntype support for numbers with string formatting clause
    #[should_panic]
    #[test]
    fn dyntype_support_for_numbers_with_string_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%s, %s".format([dyn(32), dyn(56.8)])'
                value: {
                  string_value: '32, 56.8',
                }
            "#
        ));
    }

    // Test: dyntype support for integer formatting clause
    #[should_panic]
    #[test]
    fn dyntype_support_for_integer_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([dyn(128)])'
                value: {
                  string_value: '128',
                }
            "#
        ));
    }

    // Test: dyntype support for integer formatting clause (unsigned)
    #[should_panic]
    #[test]
    fn dyntype_support_for_integer_formatting_clause_unsigned() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([dyn(256u)])'
                value: {
                  string_value: '256',
                }
            "#
        ));
    }

    // Test: dyntype support for hex formatting clause
    #[should_panic]
    #[test]
    fn dyntype_support_for_hex_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%x".format([dyn(22)])'
                value: {
                  string_value: '16',
                }
            "#
        ));
    }

    // Test: dyntype support for hex formatting clause (uppercase)
    #[should_panic]
    #[test]
    fn dyntype_support_for_hex_formatting_clause_uppercase() {
        run_test(&dedent!(
            r#"
                expr: '"%X".format([dyn(26)])'
                value: {
                  string_value: '1A',
                }
            "#
        ));
    }

    // Test: dyntype support for unsigned hex formatting clause
    #[should_panic]
    #[test]
    fn dyntype_support_for_unsigned_hex_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%x".format([dyn(500u)])'
                value: {
                  string_value: '1f4',
                }
            "#
        ));
    }

    // Test: dyntype support for fixed-point formatting clause
    #[should_panic]
    #[test]
    fn dyntype_support_for_fixed_point_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%.3f".format([dyn(4.5)])'
                value: {
                  string_value: '4.500',
                }
            "#
        ));
    }

    // Test: dyntype support for scientific notation
    #[should_panic]
    #[test]
    fn dyntype_support_for_scientific_notation() {
        run_test(&dedent!(
            r#"
                expr: '"%e".format([dyn(2.71828)])'
                value: {
                  string_value: '2.718280e+00',
                }
            "#
        ));
    }

    // Test: dyntype NaN/infinity support
    #[should_panic]
    #[test]
    fn dyntype_nan_infinity_support() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([[double("NaN"), double("Infinity"), double("-Infinity")]])'
                value: {
                  string_value: '[NaN, Infinity, -Infinity]',
                }
            "#
        ));
    }

    // Test: dyntype support for timestamp
    #[should_panic]
    #[test]
    fn dyntype_support_for_timestamp() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([dyn(timestamp("2009-11-10T23:00:00Z"))])'
                value: {
                  string_value: '2009-11-10T23:00:00Z',
                }
            "#
        ));
    }

    // Test: dyntype support for duration
    #[should_panic]
    #[test]
    fn dyntype_support_for_duration() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([dyn(duration("8747s"))])'
                value: {
                  string_value: '8747s',
                }
            "#
        ));
    }

    // Test: dyntype support for lists
    #[should_panic]
    #[test]
    fn dyntype_support_for_lists() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([dyn([6, 4.2, "a string"])])'
                value: {
                  string_value: '[6, 4.2, a string]',
                }
            "#
        ));
    }

    // Test: dyntype support for maps
    #[should_panic]
    #[test]
    fn dyntype_support_for_maps() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([{"strKey":"x", 6:duration("422s"), true:42}])'
                value: {
                  string_value: '{6: 422s, strKey: x, true: 42}',
                }
            "#
        ));
    }

    // Test: string substitution in a string variable
    #[should_panic]
    #[test]
    fn string_substitution_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format(["filler"])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%s" } }
                }
                value: {
                  string_value: 'filler',
                }
            "#
        ));
    }

    // Test: multiple substitutions in a string variable
    #[should_panic]
    #[test]
    fn multiple_substitutions_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format([1, 2, 3, "A", "B", "C", 4, 5, 6, "D", "E", "F"])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%d %d %d, %s %s %s, %d %d %d, %s %s %s" } }
                }
                value: {
                  string_value: '1 2 3, A B C, 4 5 6, D E F',
                }
            "#
        ));
    }

    // Test: substitution inside escaped percent signs in a string variable
    #[should_panic]
    #[test]
    fn substitution_inside_escaped_percent_signs_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format(["text"])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%%%s%%" } }
                }
                value: {
                  string_value: '%text%',
                }
            "#
        ));
    }

    // Test: fixed point formatting clause in a string variable
    #[should_panic]
    #[test]
    fn fixed_point_formatting_clause_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format([1.2345])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%.3f" } }
                }
                value: {
                  string_value: '1.234',
                }
            "#
        ));
    }

    // Test: binary formatting clause in a string variable
    #[should_panic]
    #[test]
    fn binary_formatting_clause_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format([5])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%b" } }
                }
                value: {
                  string_value: '101',
                }
            "#
        ));
    }

    // Test: scientific notation formatting clause in a string variable
    #[should_panic]
    #[test]
    fn scientific_notation_formatting_clause_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format([1052.032911275])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%.6e" } }
                }
                value: {
                  string_value: '1.052033e+03',
                }
            "#
        ));
    }

    // Test: default precision for fixed-point clause in a string variable
    #[should_panic]
    #[test]
    fn default_precision_for_fixed_point_clause_in_a_string_variable() {
        run_test(&dedent!(
            r#"
                expr: 'str_var.format([2.71828])'
                type_env: {
                  name: "str_var",
                  ident: { type: { primitive: STRING } }
                }
                bindings: {
                  key: "str_var"
                  value: { value: { string_value: "%f" } }
                }
                value: {
                  string_value: '2.718280',
                }
            "#
        ));
    }

    // Test: format_%f_insignificant_zeroes_removed
    #[should_panic]
    #[test]
    fn format_f_insignificant_zeroes_removed() {
        run_test(&dedent!(
            r#"
                expr: '"%.0f".format([123.000000])'
                value: {
                  string_value: '123',
                }
            "#
        ));
    }

    // Test: format_%f_positive_round_to_whole_number
    #[should_panic]
    #[test]
    fn format_f_positive_round_to_whole_number() {
        run_test(&dedent!(
            r#"
                expr: '"%.0f".format([3.5001])'
                value: {
                  string_value: '4',
                }
            "#
        ));
    }

    // Test: format_%f_negative_truncate_to_whole_number
    #[should_panic]
    #[test]
    fn format_f_negative_truncate_to_whole_number() {
        run_test(&dedent!(
            r#"
                expr: '"%.0f".format([3.4999])'
                value: {
                  string_value: '3',
                }
            "#
        ));
    }

    // Test: format_%f_halfway_round_up_to_nearest_even
    #[should_panic]
    #[test]
    fn format_f_halfway_round_up_to_nearest_even() {
        run_test(&dedent!(
            r#"
                expr: '"%.0f".format([1.5])'
                value: {
                  string_value: '2',
                }
            "#
        ));
    }

    // Test: format_%f_halfway_truncate_to_nearest_even
    #[should_panic]
    #[test]
    fn format_f_halfway_truncate_to_nearest_even() {
        run_test(&dedent!(
            r#"
                expr: '"%.0f".format([2.5])'
                value: {
                  string_value: '2',
                }
            "#
        ));
    }

    // Test: format_%f_positive_round_up
    #[should_panic]
    #[test]
    fn format_f_positive_round_up() {
        run_test(&dedent!(
            r#"
                expr: '"%.3f".format([123.4999])'
                value: {
                  string_value: '123.500',
                }
            "#
        ));
    }

    // Test: format_%f_positive_round_down
    #[should_panic]
    #[test]
    fn format_f_positive_round_down() {
        run_test(&dedent!(
            r#"
                expr: '"%.3f".format([123.4994])'
                value: {
                  string_value: '123.499',
                }
            "#
        ));
    }

    // Test: format_%f_negative_round_up
    #[should_panic]
    #[test]
    fn format_f_negative_round_up() {
        run_test(&dedent!(
            r#"
                expr: '"%.3f".format([-123.4999])'
                value: {
                  string_value: '-123.500',
                }
            "#
        ));
    }

    // Test: format_%f_negative_round_down
    #[should_panic]
    #[test]
    fn format_f_negative_round_down() {
        run_test(&dedent!(
            r#"
                expr: '"%.3f".format([-123.4994])'
                value: {
                  string_value: '-123.499',
                }
            "#
        ));
    }

    // Test: format_%f_zero_padding
    #[should_panic]
    #[test]
    fn format_f_zero_padding() {
        run_test(&dedent!(
            r#"
                expr: '"%.5f".format([-1.2])'
                value: {
                  string_value: '-1.20000',
                }
            "#
        ));
    }
}

// Section: format_errors
mod format_errors {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: unrecognized formatting clause
    #[test]
    fn unrecognized_formatting_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%a".format([1])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'could not parse formatting clause: unrecognized formatting clause "a"'
                  }
                }
            "#
        ));
    }

    // Test: out of bounds arg index
    #[test]
    fn out_of_bounds_arg_index() {
        run_test(&dedent!(
            r#"
                expr: '"%d %d %d".format([0, 1])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'index 2 out of range'
                  }
                }
            "#
        ));
    }

    // Test: string substitution is not allowed with binary clause
    #[test]
    fn string_substitution_is_not_allowed_with_binary_clause() {
        run_test(&dedent!(
            r#"
                expr: '"string is %b".format(["abc"])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: only integers and bools can be formatted as binary, was given string'
                  }
                }
            "#
        ));
    }

    // Test: duration substitution not allowed with decimal clause
    #[test]
    fn duration_substitution_not_allowed_with_decimal_clause() {
        run_test(&dedent!(
            r#"
                expr: '"%d".format([duration("30m2s")])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: decimal clause can only be used on integers, was given google.protobuf.Duration'
                  }
                }
            "#
        ));
    }

    // Test: string substitution not allowed with octal clause
    #[test]
    fn string_substitution_not_allowed_with_octal_clause() {
        run_test(&dedent!(
            r#"
                expr: '"octal: %o".format(["a string"])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: octal clause can only be used on integers, was given string'
                  }
                }
            "#
        ));
    }

    // Test: double substitution not allowed with hex clause
    #[test]
    fn double_substitution_not_allowed_with_hex_clause() {
        run_test(&dedent!(
            r#"
                expr: '"double is %x".format([0.5])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: only integers, byte buffers, and strings can be formatted as hex, was given double'
                  }
                }
            "#
        ));
    }

    // Test: uppercase not allowed for scientific clause
    #[test]
    fn uppercase_not_allowed_for_scientific_clause() {
        run_test(&dedent!(
            r#"
                expr: '"double is %E".format([0.5])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'could not parse formatting clause: unrecognized formatting clause "E"'
                  }
                }
            "#
        ));
    }

    // Test: object not allowed
    #[test]
    fn object_not_allowed() {
        run_test(&dedent!(
            r#"
                expr: '"object is %s".format([cel.expr.conformance.proto3.TestAllTypes{}])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: string clause can only be used on strings, bools, bytes, ints, doubles, maps, lists, types, durations, and timestamps, was given cel.expr.conformance.proto3.TestAllTypes'
                  }
                }
            "#
        ));
    }

    // Test: object inside list
    #[test]
    fn object_inside_list() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([[1, 2, cel.expr.conformance.proto3.TestAllTypes{}]])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: string clause can only be used on strings, bools, bytes, ints, doubles, maps, lists, types, durations, and timestamps, was given cel.expr.conformance.proto3.TestAllTypes'
                  }
                }
            "#
        ));
    }

    // Test: object inside map
    #[test]
    fn object_inside_map() {
        run_test(&dedent!(
            r#"
                expr: '"%s".format([{1: "a", 2: cel.expr.conformance.proto3.TestAllTypes{}}])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: string clause can only be used on strings, bools, bytes, ints, doubles, maps, lists, types, durations, and timestamps, was given cel.expr.conformance.proto3.TestAllTypes'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %d
    #[test]
    fn null_not_allowed_for_d() {
        run_test(&dedent!(
            r#"
                expr: '"null: %d".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: decimal clause can only be used on integers, was given null_type'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %e
    #[test]
    fn null_not_allowed_for_e() {
        run_test(&dedent!(
            r#"
                expr: '"null: %e".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: scientific clause can only be used on doubles, was given null_type'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %f
    #[test]
    fn null_not_allowed_for_f() {
        run_test(&dedent!(
            r#"
                expr: '"null: %f".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: fixed-point clause can only be used on doubles, was given null_type'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %x
    #[test]
    fn null_not_allowed_for_x() {
        run_test(&dedent!(
            r#"
                expr: '"null: %x".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: only integers, byte buffers, and strings can be formatted as hex, was given null_type'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %X
    #[test]
    fn null_not_allowed_for_x_1() {
        run_test(&dedent!(
            r#"
                expr: '"null: %X".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: only integers, byte buffers, and strings can be formatted as hex, was given null_type'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %b
    #[test]
    fn null_not_allowed_for_b() {
        run_test(&dedent!(
            r#"
                expr: '"null: %b".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: only integers and bools can be formatted as binary, was given null_type'
                  }
                }
            "#
        ));
    }

    // Test: null not allowed for %o
    #[test]
    fn null_not_allowed_for_o() {
        run_test(&dedent!(
            r#"
                expr: '"null: %o".format([null])'
                disable_check: true
                eval_error: {
                  errors: {
                    message: 'error during formatting: octal clause can only be used on integers, was given null_type'
                  }
                }
            "#
        ));
    }
}

// Section: value_errors
mod value_errors {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: charat_out_of_range
    #[test]
    fn charat_out_of_range() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.charAt(30) == ''"
                eval_error: {
                  errors: {
                    message: "index out of range: 30"
                  }
                }
            "#
        ));
    }

    // Test: indexof_out_of_range
    #[test]
    fn indexof_out_of_range() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.indexOf('a', 30) == -1"
                eval_error: {
                  errors: {
                    message: "index out of range: 30"
                  }
                }
            "#
        ));
    }

    // Test: lastindexof_negative_index
    #[test]
    fn lastindexof_negative_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('a', -1) == -1"
                eval_error: {
                  errors: {
                    message: "index out of range: -1"
                  }
                }
            "#
        ));
    }

    // Test: lastindexof_out_of_range
    #[test]
    fn lastindexof_out_of_range() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.lastIndexOf('a', 30) == -1"
                eval_error: {
                  errors: {
                    message: "index out of range: 30"
                  }
                }
            "#
        ));
    }

    // Test: substring_out_of_range
    #[test]
    fn substring_out_of_range() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(40) == 'cat'"
                eval_error: {
                  errors: {
                    message: "index out of range: 40"
                  }
                }
            "#
        ));
    }

    // Test: substring_negative_index
    #[test]
    fn substring_negative_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(-1) == 'cat'"
                eval_error: {
                  errors: {
                    message: "index out of range: -1"
                  }
                }
            "#
        ));
    }

    // Test: substring_end_index_out_of_range
    #[test]
    fn substring_end_index_out_of_range() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(1, 50) == 'cat'"
                eval_error: {
                  errors: {
                    message: "index out of range: 50"
                  }
                }
            "#
        ));
    }

    // Test: substring_begin_index_out_of_range
    #[test]
    fn substring_begin_index_out_of_range() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(49, 50) == 'cat'"
                eval_error: {
                  errors: {
                    message: "index out of range: 49"
                  }
                }
            "#
        ));
    }

    // Test: substring_end_index_greater_than_begin_index
    #[test]
    fn substring_end_index_greater_than_begin_index() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(4, 3) == ''"
                eval_error: {
                  errors: {
                    message: "invalid substring range. start: 4, end: 3"
                  }
                }
            "#
        ));
    }
}

// Section: type_errors
mod type_errors {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: charat_invalid_type
    #[test]
    fn charat_invalid_type() {
        run_test(&dedent!(
            r#"
                expr: "42.charAt(2) == ''"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: charat_invalid_argument
    #[test]
    fn charat_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'hello'.charAt(true) == ''"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: indexof_unary_invalid_type
    #[test]
    fn indexof_unary_invalid_type() {
        run_test(&dedent!(
            r#"
                expr: "24.indexOf('2') == 0"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: indexof_unary_invalid_argument
    #[test]
    fn indexof_unary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'hello'.indexOf(true) == 1"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: indexof_binary_invalid_argument
    #[test]
    fn indexof_binary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "42.indexOf('4', 0) == 0"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: indexof_binary_invalid_argument_2
    #[test]
    fn indexof_binary_invalid_argument_2() {
        run_test(&dedent!(
            r#"
                expr: "'42'.indexOf(4, 0) == 0"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: indexof_binary_both_invalid_arguments
    #[test]
    fn indexof_binary_both_invalid_arguments() {
        run_test(&dedent!(
            r#"
                expr: "'42'.indexOf('4', '0') == 0"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: indexof_ternary_invalid_arguments
    #[test]
    fn indexof_ternary_invalid_arguments() {
        run_test(&dedent!(
            r#"
                expr: "'42'.indexOf('4', 0, 1) == 0"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_invalid_type
    #[test]
    fn split_invalid_type() {
        run_test(&dedent!(
            r#"
                expr: "42.split('2') == ['4']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_invalid_type
    #[test]
    fn replace_invalid_type() {
        run_test(&dedent!(
            r#"
                expr: "42.replace(2, 1) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_binary_invalid_argument
    #[test]
    fn replace_binary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'42'.replace(2, 1) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_binary_invalid_argument_2
    #[test]
    fn replace_binary_invalid_argument_2() {
        run_test(&dedent!(
            r#"
                expr: "'42'.replace('2', 1) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_ternary_invalid_argument
    #[test]
    fn replace_ternary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "42.replace('2', '1', 1) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_ternary_invalid_argument_2
    #[test]
    fn replace_ternary_invalid_argument_2() {
        run_test(&dedent!(
            r#"
                expr: "'42'.replace(2, '1', 1) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_ternary_invalid_argument_3
    #[test]
    fn replace_ternary_invalid_argument_3() {
        run_test(&dedent!(
            r#"
                expr: "'42'.replace('2', 1, 1) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_ternary_invalid_argument_4
    #[test]
    fn replace_ternary_invalid_argument_4() {
        run_test(&dedent!(
            r#"
                expr: "'42'.replace('2', '1', '1') == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: replace_quaternary_invalid_argument
    #[test]
    fn replace_quaternary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'42'.replace('2', '1', 1, false) == '41'"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_invalid_type_empty_arg
    #[test]
    fn split_invalid_type_empty_arg() {
        run_test(&dedent!(
            r#"
                expr: "42.split('') == ['4', '2']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_invalid_argument
    #[test]
    fn split_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'42'.split(2) == ['4']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_binary_invalid_type
    #[test]
    fn split_binary_invalid_type() {
        run_test(&dedent!(
            r#"
                expr: "42.split('2', '1') == ['4']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_binary_invalid_argument
    #[test]
    fn split_binary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'42'.split(2, 1) == ['4']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_binary_invalid_argument_2
    #[test]
    fn split_binary_invalid_argument_2() {
        run_test(&dedent!(
            r#"
                expr: "'42'.split('2', '1') == ['4']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: split_ternary_invalid_argument
    #[test]
    fn split_ternary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'42'.split('2', 1, 1) == ['4']"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: substring_ternary_invalid_argument
    #[test]
    fn substring_ternary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'hello'.substring(1, 2, 3) == ''"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: substring_binary_invalid_type
    #[test]
    fn substring_binary_invalid_type() {
        run_test(&dedent!(
            r#"
                expr: "30.substring(true, 3) == ''"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: substring_binary_invalid_argument
    #[test]
    fn substring_binary_invalid_argument() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(true, 3) == ''"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }

    // Test: substring_binary_invalid_argument_2
    #[test]
    fn substring_binary_invalid_argument_2() {
        run_test(&dedent!(
            r#"
                expr: "'tacocat'.substring(0, false) == ''"
                disable_check: true
                eval_error: {
                  errors: {
                    message: "no such overload"
                  }
                }
            "#
        ));
    }
}
