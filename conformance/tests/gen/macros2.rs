// @generated by `cargo run -p conformance --bin generate`.
// DO NOT EDIT MANUALLY.

// Source file: macros2
// Description: Tests for CEL comprehensions v2

// Section: exists
// Tests for the .exists() macro, which is equivalent to joining the evaluated elements with logical-OR.
mod exists {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: list_elem_all_true
    #[should_panic]
    #[test]
    fn list_elem_all_true() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].exists(i, v, i > -1 && v > 0)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: list_elem_some_true
    #[should_panic]
    #[test]
    fn list_elem_some_true() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].exists(i, v, i == 1 && v == 2)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: list_elem_none_true
    #[should_panic]
    #[test]
    fn list_elem_none_true() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].exists(i, v, i > 2 && v > 3)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_type_shortcircuit
    #[should_panic]
    #[test]
    fn list_elem_type_shortcircuit() {
        run_test(&dedent!(
            r#"
                expr: "[1, 'foo', 3].exists(i, v, i == 1 && v != '1')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: list_elem_type_exhaustive
    #[should_panic]
    #[test]
    fn list_elem_type_exhaustive() {
        run_test(&dedent!(
            r#"
                expr: "[1, 'foo', 3].exists(i, v, i == 3 || v == '10')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_exists_error
    #[test]
    fn list_elem_exists_error() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].exists(i, v, v / i == 17)"
                eval_error: {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }

    // Test: list_empty
    #[should_panic]
    #[test]
    fn list_empty() {
        run_test(&dedent!(
            r#"
                expr: "[].exists(i, v, i == 0 || v == 2)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: map_key
    #[should_panic]
    #[test]
    fn map_key() {
        run_test(&dedent!(
            r#"
                expr: "{'key1':1, 'key2':2}.exists(k, v, k == 'key2' && v == 2)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: not_map_key
    #[should_panic]
    #[test]
    fn not_map_key() {
        run_test(&dedent!(
            r#"
                expr: "!{'key1':1, 'key2':2}.exists(k, v, k == 'key3' || v == 3)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: map_key_type_shortcircuit
    #[should_panic]
    #[test]
    fn map_key_type_shortcircuit() {
        run_test(&dedent!(
            r#"
                expr: "{'key':1, 1:21}.exists(k, v, k != 2 && v != 22)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: map_key_type_exhaustive
    #[should_panic]
    #[test]
    fn map_key_type_exhaustive() {
        run_test(&dedent!(
            r#"
                expr: "!{'key':1, 1:42}.exists(k, v, k == 2 && v == 43)"
                value: { bool_value: true }
            "#
        ));
    }
}

// Section: all
// Tests for the .all() macro, which is equivalent to joining the evaluated elements with logical-AND.
mod all {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: list_elem_all_true
    #[should_panic]
    #[test]
    fn list_elem_all_true() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].all(i, v, i > -1 && v > 0)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: list_elem_some_true
    #[should_panic]
    #[test]
    fn list_elem_some_true() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].all(i, v, i == 1 && v == 2)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_none_true
    #[should_panic]
    #[test]
    fn list_elem_none_true() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].all(i, v, i == 3 || v == 4)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_type_shortcircuit
    #[should_panic]
    #[test]
    fn list_elem_type_shortcircuit() {
        run_test(&dedent!(
            r#"
                expr: "[1, 'foo', 3].all(i, v, i == 0 || v == 1)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_type_exhaustive
    #[should_panic]
    #[test]
    fn list_elem_type_exhaustive() {
        run_test(&dedent!(
            r#"
                expr: "[0, 'foo', 3].all(i, v, v % 2 == i)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_type_error_exhaustive
    #[test]
    fn list_elem_type_error_exhaustive() {
        run_test(&dedent!(
            r#"
                expr: "[0, 'foo', 5].all(i, v, v % 3 == i)"
                eval_error: {
                  errors: { message: "no_such_overload" }
                }
            "#
        ));
    }

    // Test: list_elem_error_shortcircuit
    #[should_panic]
    #[test]
    fn list_elem_error_shortcircuit() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].all(i, v, 6 / (2 - v) == i)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_elem_error_exhaustive
    #[test]
    fn list_elem_error_exhaustive() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].all(i, v, v / i != 17)"
                eval_error: {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }

    // Test: list_empty
    #[should_panic]
    #[test]
    fn list_empty() {
        run_test(&dedent!(
            r#"
                expr: "[].all(i, v, i > -1 || v > 0)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: map_key
    #[should_panic]
    #[test]
    fn map_key() {
        run_test(&dedent!(
            r#"
                expr: "{'key1':1, 'key2':2}.all(k, v, k == 'key2' && v == 2)"
                value: { bool_value: false }
            "#
        ));
    }
}

// Section: existsOne
// Tests for existsOne() macro. An expression 'L.existsOne(I, E)' is equivalent to 'size(L.filter(I, E)) == 1'.
mod existsone {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: list_empty
    #[should_panic]
    #[test]
    fn list_empty() {
        run_test(&dedent!(
            r#"
                expr: "[].existsOne(i, v, i == 3 || v == 7)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_one_true
    #[should_panic]
    #[test]
    fn list_one_true() {
        run_test(&dedent!(
            r#"
                expr: "[7].existsOne(i, v, i == 0 && v == 7)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: list_one_false
    #[should_panic]
    #[test]
    fn list_one_false() {
        run_test(&dedent!(
            r#"
                expr: "[8].existsOne(i, v, i == 0 && v == 7)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_none
    #[should_panic]
    #[test]
    fn list_none() {
        run_test(&dedent!(
            r#"
                expr: "[1, 2, 3].existsOne(i, v, i > 2 || v > 3)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_one
    #[should_panic]
    #[test]
    fn list_one() {
        run_test(&dedent!(
            r#"
                expr: "[5, 7, 8].existsOne(i, v, v % 5 == i)"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: list_many
    #[should_panic]
    #[test]
    fn list_many() {
        run_test(&dedent!(
            r#"
                expr: "[0, 1, 2, 3, 4].existsOne(i, v, v % 2 == i)"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_all
    #[should_panic]
    #[test]
    fn list_all() {
        run_test(&dedent!(
            r#"
                expr: "['foal', 'foo', 'four'].existsOne(i, v, i > -1 && v.startsWith('fo'))"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: list_no_shortcircuit
    #[test]
    fn list_no_shortcircuit() {
        run_test(&dedent!(
            r#"
                expr: "[3, 2, 1, 0].existsOne(i, v, v / i > 1)"
                eval_error {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }

    // Test: map_one
    #[should_panic]
    #[test]
    fn map_one() {
        run_test(&dedent!(
            r#"
                expr: "{6: 'six', 7: 'seven', 8: 'eight'}.existsOne(k, v, k % 5 == 2 && v == 'seven')"
                value: { bool_value: true }
            "#
        ));
    }
}

// Section: transformList
// Tests for transformList() macro.
mod transformlist {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty
    #[should_panic]
    #[test]
    fn empty() {
        run_test(&dedent!(
            r#"
                expr: "[].transformList(i, v, i / v)"
                value: { list_value: {} }
            "#
        ));
    }

    // Test: empty_filter
    #[should_panic]
    #[test]
    fn empty_filter() {
        run_test(&dedent!(
            r#"
                expr: "[].transformList(i, v, i > v, i / v)"
                value: { list_value: {} }
            "#
        ));
    }

    // Test: one
    #[should_panic]
    #[test]
    fn one() {
        run_test(&dedent!(
            r#"
                expr: "[3].transformList(i, v, v * v + i)"
                value: { list_value {
                    values: { int64_value: 9 }
                  }
                }
            "#
        ));
    }

    // Test: one_filter
    #[should_panic]
    #[test]
    fn one_filter() {
        run_test(&dedent!(
            r#"
                expr: "[3].transformList(i, v, i == 0 && v == 3, v * v + i)"
                value: { list_value {
                    values: { int64_value: 9 }
                  }
                }
            "#
        ));
    }

    // Test: many
    #[should_panic]
    #[test]
    fn many() {
        run_test(&dedent!(
            r#"
                expr: "[2, 4, 6].transformList(i, v, v / 2 + i)"
                value: { list_value {
                    values: { int64_value: 1 }
                    values: { int64_value: 3 }
                    values: { int64_value: 5 }
                  }
                }
            "#
        ));
    }

    // Test: many_filter
    #[should_panic]
    #[test]
    fn many_filter() {
        run_test(&dedent!(
            r#"
                expr: "[2, 4, 6].transformList(i, v, i != 1 && v != 4, v / 2 + i)"
                value: { list_value {
                    values: { int64_value: 1 }
                    values: { int64_value: 5 }
                  }
                }
            "#
        ));
    }

    // Test: error
    #[test]
    fn error() {
        run_test(&dedent!(
            r#"
                expr: "[2, 1, 0].transformList(i, v, v / i)"
                eval_error {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }

    // Test: error_filter
    #[test]
    fn error_filter() {
        run_test(&dedent!(
            r#"
                expr: "[2, 1, 0].transformList(i, v, v / i > 0, v)"
                eval_error {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }
}

// Section: transformMap
// Tests for transformMap() macro.
mod transformmap {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty
    #[should_panic]
    #[test]
    fn empty() {
        run_test(&dedent!(
            r#"
                expr: "{}.transformMap(k, v, k + v)"
                value: { map_value: {} }
            "#
        ));
    }

    // Test: empty_filter
    #[should_panic]
    #[test]
    fn empty_filter() {
        run_test(&dedent!(
            r#"
                expr: "{}.transformMap(k, v, k == 'foo' && v == 'bar', k + v)"
                value: { map_value: {} }
            "#
        ));
    }

    // Test: one
    #[should_panic]
    #[test]
    fn one() {
        run_test(&dedent!(
            r#"
                expr: "{'foo': 'bar'}.transformMap(k, v, k + v)"
                value: {
                  map_value: {
                    entries: {
                      key: {
                        string_value: "foo"
                      }
                      value: {
                        string_value: "foobar"
                      }
                    }
                  }
                }
            "#
        ));
    }

    // Test: one_filter
    #[should_panic]
    #[test]
    fn one_filter() {
        run_test(&dedent!(
            r#"
                expr: "{'foo': 'bar'}.transformMap(k, v, k == 'foo' && v == 'bar', k + v)"
                value: {
                  map_value: {
                    entries: {
                      key: {
                        string_value: "foo"
                      }
                      value: {
                        string_value: "foobar"
                      }
                    }
                  }
                }
            "#
        ));
    }

    // Test: many
    #[should_panic]
    #[test]
    fn many() {
        run_test(&dedent!(
            r#"
                expr: "{'foo': 'bar', 'baz': 'bux', 'hello': 'world'}.transformMap(k, v, k + v)"
                value: {
                  map_value: {
                    entries: {
                      key: {
                        string_value: "foo"
                      }
                      value: {
                        string_value: "foobar"
                      }
                    }
                    entries: {
                      key: {
                        string_value: "baz"
                      }
                      value: {
                        string_value: "bazbux"
                      }
                    }
                    entries: {
                      key: {
                        string_value: "hello"
                      }
                      value: {
                        string_value: "helloworld"
                      }
                    }
                  }
                }
            "#
        ));
    }

    // Test: many_filter
    #[should_panic]
    #[test]
    fn many_filter() {
        run_test(&dedent!(
            r#"
                expr: "{'foo': 'bar', 'baz': 'bux', 'hello': 'world'}.transformMap(k, v, k != 'baz' && v != 'bux', k + v)"
                value: {
                  map_value: {
                    entries: {
                      key: {
                        string_value: "foo"
                      }
                      value: {
                        string_value: "foobar"
                      }
                    }
                    entries: {
                      key: {
                        string_value: "hello"
                      }
                      value: {
                        string_value: "helloworld"
                      }
                    }
                  }
                }
            "#
        ));
    }

    // Test: error
    #[test]
    fn error() {
        run_test(&dedent!(
            r#"
                expr: "{'foo': 2, 'bar': 1, 'baz': 0}.transformMap(k, v, 4 / v)"
                eval_error {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }

    // Test: error_filter
    #[test]
    fn error_filter() {
        run_test(&dedent!(
            r#"
                expr: "{'foo': 2, 'bar': 1, 'baz': 0}.transformMap(k, v, k == 'baz' && 4 / v == 0, v)"
                eval_error {
                  errors: { message: "divide by zero" }
                }
            "#
        ));
    }
}
