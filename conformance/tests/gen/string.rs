// @generated by `cargo run -p conformance --bin generate`.
// DO NOT EDIT MANUALLY.

// Source file: string
// Description: Tests for string and bytes operations.

// Section: size
// Tests for the size() function.
mod size {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: empty
    #[test]
    fn empty() {
        run_test(&dedent!(
            r#"
                expr: "size('')"
                value: { int64_value: 0 }
            "#
        ));
    }

    // Test: one_ascii
    #[test]
    fn one_ascii() {
        run_test(&dedent!(
            r#"
                expr: "size('A')"
                value: { int64_value: 1 }
            "#
        ));
    }

    // Test: one_unicode
    #[ignore]
    #[test]
    fn one_unicode() {
        run_test(&dedent!(
            r#"
                expr: "size('ÿ')"
                value: { int64_value: 1 }
            "#
        ));
    }

    // Test: ascii
    #[test]
    fn ascii() {
        run_test(&dedent!(
            r#"
                expr: "size('four')"
                value: { int64_value: 4 }
            "#
        ));
    }

    // Test: unicode
    #[ignore]
    #[test]
    fn unicode() {
        run_test(&dedent!(
            r#"
                expr: "size('πέντε')"
                value: { int64_value: 5 }
            "#
        ));
    }

    // Test: bytes_empty
    #[test]
    fn bytes_empty() {
        run_test(&dedent!(
            r#"
                expr: "size(b'')"
                value: { int64_value: 0 }
            "#
        ));
    }

    // Test: bytes
    #[test]
    fn bytes() {
        run_test(&dedent!(
            r#"
                expr: "size(b'abc')"
                value: { int64_value: 3 }
            "#
        ));
    }
}

// Section: starts_with
// Tests for the startsWith() function.
mod starts_with {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: basic_true
    #[test]
    fn basic_true() {
        run_test(&dedent!(
            r#"
                expr: "'foobar'.startsWith('foo')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: basic_false
    #[test]
    fn basic_false() {
        run_test(&dedent!(
            r#"
                expr: "'foobar'.startsWith('bar')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: empty_target
    #[test]
    fn empty_target() {
        run_test(&dedent!(
            r#"
                expr: "''.startsWith('foo')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: empty_arg
    #[test]
    fn empty_arg() {
        run_test(&dedent!(
            r#"
                expr: "'foobar'.startsWith('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: empty_empty
    #[test]
    fn empty_empty() {
        run_test(&dedent!(
            r#"
                expr: "''.startsWith('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: unicode
    #[test]
    fn unicode() {
        run_test(&dedent!(
            r#"
                expr: "'завтра'.startsWith('за')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: unicode_smp
    #[test]
    fn unicode_smp() {
        run_test(&dedent!(
            r#"
                expr: "'\U0001F431\U0001F600\U0001F61B'.startsWith('\U0001F431')"
                value: { bool_value: true }
            "#
        ));
    }
}

// Section: ends_with
// Tests for the endsWith() function.
mod ends_with {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: basic_true
    #[test]
    fn basic_true() {
        run_test(&dedent!(
            r#"
                expr: "'foobar'.endsWith('bar')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: basic_false
    #[test]
    fn basic_false() {
        run_test(&dedent!(
            r#"
                expr: "'foobar'.endsWith('foo')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: empty_target
    #[test]
    fn empty_target() {
        run_test(&dedent!(
            r#"
                expr: "''.endsWith('foo')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: empty_arg
    #[test]
    fn empty_arg() {
        run_test(&dedent!(
            r#"
                expr: "'foobar'.endsWith('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: empty_empty
    #[test]
    fn empty_empty() {
        run_test(&dedent!(
            r#"
                expr: "''.endsWith('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: unicode
    #[test]
    fn unicode() {
        run_test(&dedent!(
            r#"
                expr: "'forté'.endsWith('té')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: unicode_smp
    #[test]
    fn unicode_smp() {
        run_test(&dedent!(
            r#"
                expr: "'\U0001F431\U0001F600\U0001F61B'.endsWith('\U0001F61B')"
                value: { bool_value: true }
            "#
        ));
    }
}

// Section: matches
// Tests for regexp matching.  For now, we will only test the subset of regular languages.
mod matches {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: basic
    #[test]
    fn basic() {
        run_test(&dedent!(
            r#"
                expr: "'hubba'.matches('ubb')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: empty_target
    #[test]
    fn empty_target() {
        run_test(&dedent!(
            r#"
                expr: "''.matches('foo|bar')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: empty_arg
    #[test]
    fn empty_arg() {
        run_test(&dedent!(
            r#"
                expr: "'cows'.matches('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: empty_empty
    #[test]
    fn empty_empty() {
        run_test(&dedent!(
            r#"
                expr: "''.matches('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: re_concat
    #[test]
    fn re_concat() {
        run_test(&dedent!(
            r#"
                expr: "'abcd'.matches('bc')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: re_alt
    #[test]
    fn re_alt() {
        run_test(&dedent!(
            r#"
                expr: "'grey'.matches('gr(a|e)y')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: re_rep
    #[test]
    fn re_rep() {
        run_test(&dedent!(
            r#"
                expr: "'banana'.matches('ba(na)*')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: unicode
    #[test]
    fn unicode() {
        run_test(&dedent!(
            r#"
                expr: "'mañana'.matches('a+ñ+a+')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: unicode_smp
    #[test]
    fn unicode_smp() {
        run_test(&dedent!(
            r#"
                expr: "'\U0001F431\U0001F600\U0001F600'.matches('(a|\U0001F600){2}')"
                value: { bool_value: true }
            "#
        ));
    }
}

// Section: concatenation
// Tests for string concatenation.
mod concatenation {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: concat_true
    #[test]
    fn concat_true() {
        run_test(&dedent!(
            r#"
                expr: "'he' + 'llo'"
                value: { string_value: "hello" }
            "#
        ));
    }

    // Test: concat_with_spaces
    #[test]
    fn concat_with_spaces() {
        run_test(&dedent!(
            r#"
                expr: "'hello' + ' ' == 'hello'"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: concat_empty_string_beginning
    #[test]
    fn concat_empty_string_beginning() {
        run_test(&dedent!(
            r#"
                expr: "'' + 'abc'"
                value: { string_value: "abc" }
            "#
        ));
    }

    // Test: concat_empty_string_end
    #[test]
    fn concat_empty_string_end() {
        run_test(&dedent!(
            r#"
                expr: "'abc' + ''"
                value: { string_value: "abc" }
            "#
        ));
    }

    // Test: concat_empty_with_empty
    #[test]
    fn concat_empty_with_empty() {
        run_test(&dedent!(
            r#"
                expr: "'' + ''"
                value: { string_value: "" }
            "#
        ));
    }

    // Test: unicode_unicode
    #[test]
    fn unicode_unicode() {
        run_test(&dedent!(
            r#"
                expr: "'¢' + 'ÿ' + 'Ȁ'"
                value: { string_value: "¢ÿȀ" }
            "#
        ));
    }

    // Test: ascii_unicode
    #[test]
    fn ascii_unicode() {
        run_test(&dedent!(
            r#"
                expr: "'r' + 'ô' + 'le'"
                value: { string_value: "rôle" }
            "#
        ));
    }

    // Test: ascii_unicode_unicode_smp
    #[test]
    fn ascii_unicode_unicode_smp() {
        run_test(&dedent!(
            r#"
                expr: "'a' + 'ÿ' + '\U0001F431'"
                value: { string_value: "aÿ\xf0\x9f\x90\xb1" }
            "#
        ));
    }

    // Test: empty_unicode
    #[test]
    fn empty_unicode() {
        run_test(&dedent!(
            r#"
                expr: "'' + 'Ω' + ''"
                value: { string_value: "Ω" }
            "#
        ));
    }
}

// Section: contains
// Tests for contains.
mod contains {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: contains_true
    #[test]
    fn contains_true() {
        run_test(&dedent!(
            r#"
                expr: "'hello'.contains('he')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: contains_empty
    #[test]
    fn contains_empty() {
        run_test(&dedent!(
            r#"
                expr: "'hello'.contains('')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: contains_false
    #[test]
    fn contains_false() {
        run_test(&dedent!(
            r#"
                expr: "'hello'.contains('ol')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: contains_multiple
    #[test]
    fn contains_multiple() {
        run_test(&dedent!(
            r#"
                expr: "'abababc'.contains('ababc')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: contains_unicode
    #[test]
    fn contains_unicode() {
        run_test(&dedent!(
            r#"
                expr: "'Straße'.contains('aß')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: contains_unicode_smp
    #[test]
    fn contains_unicode_smp() {
        run_test(&dedent!(
            r#"
                expr: "'\U0001F431\U0001F600\U0001F601'.contains('\U0001F600')"
                value: { bool_value: true }
            "#
        ));
    }

    // Test: empty_contains
    #[test]
    fn empty_contains() {
        run_test(&dedent!(
            r#"
                expr: "''.contains('something')"
                value: { bool_value: false }
            "#
        ));
    }

    // Test: empty_empty
    #[test]
    fn empty_empty() {
        run_test(&dedent!(
            r#"
                expr: "''.contains('')"
                value: { bool_value: true }
            "#
        ));
    }
}

// Section: bytes_concat
// Tests for bytes concatenation.
mod bytes_concat {
    use conformance::runner::run_test;
    use dedent::dedent;

    // Test: concat
    #[test]
    fn concat() {
        run_test(&dedent!(
            r#"
                expr: "b'abc' + b'def'"
                value: { bytes_value: "abcdef" }
            "#
        ));
    }

    // Test: left_unit
    #[test]
    fn left_unit() {
        run_test(&dedent!(
            r#"
                expr: "b'' + b'\\xffoo'"
                value: { bytes_value: "\377oo" }
            "#
        ));
    }

    // Test: right_unit
    #[test]
    fn right_unit() {
        run_test(&dedent!(
            r#"
                expr: "b'zxy' + b''"
                value: { bytes_value: "zxy" }
            "#
        ));
    }

    // Test: empty_empty
    #[test]
    fn empty_empty() {
        run_test(&dedent!(
            r#"
                expr: "b'' + b''"
                value: { bytes_value: "" }
            "#
        ));
    }
}
